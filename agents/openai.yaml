interface:
  display_name: "Novel AI Detox"
  short_description: "Controller-led fiction critique and rewrite workflow with reader and worker sub-agents"
  default_prompt: "Use $novel-ai-detox as a controller-led workflow: keep the first agent on planning, delegation, merge, and final inspection; let reader and worker sub-agents handle critique and paragraph rewrites; preserve plot facts; keep natural paragraphs short; and let the protagonist sound lazier, cockier, and more dominant when appropriate."
